---
title: Introduction
description: A minimal Papervine docs site you can copy and make your own.
---

This folder is a complete, working docs site: a `docs.json` and a few MDX pages. Nothing
else is required.

Preview it from the repository root:

```bash
npm run dev -- examples/starter
```

Or, with the published CLI, from anywhere:

```bash
npx papervine dev
```

<Note>
  Every page renders per request and is read from disk, so save a file and refresh to see
  the change.
</Note>

## Make it yours

<Steps>
  <Step title="Copy the folder">
    Copy `examples/starter` to wherever your docs should live, or start from it in a fresh
    repository.
  </Step>
  <Step title="Edit docs.json">
    Set `name`, pick your `colors`, and lay out the `navigation` tree. Every page in the
    tree is a `.mdx` file at the matching path.
  </Step>
  <Step title="Write pages">
    Add `.mdx` files with `title` and `description` frontmatter, then reference them from
    the navigation.
  </Step>
</Steps>

## What you get

<CardGroup cols={2}>
  <Card title="Navigation and search-friendly structure">
    Tabs, groups, a sidebar, and an on-page table of contents, all driven by `docs.json`.
  </Card>
  <Card title="Components">
    Callouts, cards, tabs, steps, code groups, trees, badges, diagrams and more. A few are
    shown on [Components](/components); the full gallery is in the
    [starter repo](https://github.com/papervine/starter).
  </Card>
  <Card title="Dark mode">
    Built in, with the appearance default set in `docs.json`.
  </Card>
  <Card title="API reference">
    Point `docs.json` at an OpenAPI spec and endpoint pages are generated for you.
  </Card>
</CardGroup>
