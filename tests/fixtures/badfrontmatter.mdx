---
title: "unterminated string
description: this YAML is malformed on purpose
---

The frontmatter above is invalid YAML (unterminated quote). The page must still
render (safe frontmatter parsing), not 500.

BAD_FRONTMATTER_MARKER
