---
title: Author violation
---

VIOLATION_PAGE_MARKER — this page breaks the component contract, so it must degrade to the
notice and never be evaluated on either side (SPEC §10.6).

import { execSync } from "node:child_process";
