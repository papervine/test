# node_modules / build output — match at any depth (workspace packages + apps)
node_modules/
.next/
# Per-harness build output (NEXT_DIST_DIR) so tests can run beside `npm run dev`.
.next-*/
/out
/build
*.tsbuildinfo
next-env.d.ts
seeded-dashboard.png
.DS_Store
_private/
.env*.local
.vercel

# Playwright
/test-results
/playwright-report
/playwright/.cache
/tests/e2e/.auth


# Transient agent browser-automation artifacts
.playwright-cli/
.playwright-mcp/
.env.r2.local

.projects/cache
.projects/vault
.projects/state.test.json
.projects/state.local.test.json
.env
.env.*
!.env.example

# Sentry Config File
.env.sentry-build-plugin

.trigger
# Model eval outputs (evals/run.mjs writes edited corpus copies here)
evals/.out/
