{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npx drizzle-kit migrate && next build",
  "git": { "deploymentEnabled": { "main": false } },
  "crons": [
    { "path": "/api/reconcile/domains", "schedule": "*/10 * * * *" },
    { "path": "/api/skills/generate", "schedule": "20 * * * *" }
  ]
}
