---
title: "Plans & billing"
description: "How plans, the 30-day trial, and AI credits work"
public: true
---

Billing is scoped to the **organization**: one plan and one credit pool cover every
site the org owns. It lives under a site's **Settings**, in the Workspace section:

- **Settings → Billing** — the current plan, changing plans, and downgrading.
- **Settings → Usage** — the credit meter, the reset date, the usage chart, credit
  packs, and recent AI usage.

Because the plan and credits are organization-level, every site's Billing and Usage tabs
show the same subscription.

## Plans

| | Free | Team | Pro | Enterprise |
| --- | --- | --- | --- | --- |
| Price | $0 | $65/mo ($55/mo billed annually) | $250/mo ($200/mo billed annually) | Contact us |
| BYOK assistant | Yes | Yes | Yes | Yes |
| Hosted AI credits | Trial only (10k / 30 days) | Optional pool (1k/mo) | Optional pool (25k/mo) | Committed volume optional |
| Docs sites | 1 | 3 | 10 | Custom |
| Editors | 5 | Unlimited | Unlimited | Custom |
| Analytics retention | 7 days | 90 days | 1 year | Custom |

Core docs infrastructure — Git sync, custom domains, reader authentication, the web
editor, search, analytics, the API playground, and the MCP server — is included on
**every** plan, Free onward. **BYOK AI assistant** is available from day one on all plans.
Paid plans add unlimited editors, multiplayer editing, SSO and role-based permissions from
**Team**, and the AI agent, scheduled automations, insights and white-labelling from
**Pro**. **Hosted AI credits** are optional add-ons on paid plans, not the headline
feature. Enterprise holds the procurement items: SCIM,
security and legal review, SLAs, migration, and dedicated support.

## The Papervine badge

Docs sites on every plan below Enterprise carry a small **Powered by Papervine** link at the
bottom right of the page. It's plain text, it doesn't follow the reader down the page, and it
never appears in print.

Removing it is part of **Enterprise**, alongside the other white-labelling and procurement
items. Upgrading takes it off every site the organization owns, within about a minute — the
answer is cached briefly so that reading it costs nothing on the way to a docs page.

## The 30-day trial

Every new account starts with a **30-day free trial of Pro** — every Pro feature plus a
one-time grant of **10,000 AI credits**, no card required. When the trial ends, the
organization lands on Free: docs keep rendering exactly as before, and AI features
pause until a plan is chosen. Settings → Billing shows the countdown, and the trial-gated
features carry a **Trialing** badge in the dashboard sidebar.

## AI credits

Credits are the accounting unit for AI usage. Every assistant answer, writing-agent
run, and workflow run meters the model tokens it actually used and converts them to
credits with a published rate table — a typical assistant answer costs around **10
credits**. Settings → Usage shows credits remaining, the **reset date**, and a table of
recent operations with their token count and credit cost.

### Where the credits are going

**Credit usage**, at the top of the page, charts the last 30 days: one bar per day,
stacked by the feature that spent the credits — **Assistant**, **Editor agent**, and
**Automations**. Hover a day (or focus the chart and use the arrow keys) for that day's
split, and read the strip under the chart for each feature's 30-day total and its share
of the period. A feature the organization hasn't used doesn't appear.

The credit meter and credit packs sit side by side beneath it, so the chart answers "which
feature is spending the pool" and the meter answers "how much is left". The **Recent AI
usage** table at the bottom of the page is the same story per call — the operation, the
model, the tokens, and the credits it cost.

Credits come from three buckets, consumed in this order:

1. **Trial** — the one-time signup grant (expires with the trial).
2. **Monthly** — the plan's included pool, refreshed each billing period (the "Next
   reset" date on the Usage page). Unused monthly credits don't roll over.
3. **Packs** — one-time top-ups purchased on any paid plan. Packs never expire.

## Hard caps and overage

By default a plan is **hard-capped**: when the credit pool runs out, AI features pause
until the next period (or a pack purchase) — there are no surprise bills. Owners and
admins can opt in to **overage** on the Usage page, which lets usage continue past
the included pool at the plan's per-credit overage rate (Team $0.008, Pro $0.006).

## Managing the subscription

Upgrades go through Stripe Checkout from Settings → Billing (pack purchases from
Settings → Usage). Once an organization is on a paid plan, choosing a different plan or
interval switches the existing subscription in place, with proration — no second
subscription is created.

**Downgrading to Free** is on the current-plan card: it cancels at the end of the
billing period (with a confirm step), and can be resumed any time before then. Docs
keep rendering on Free; AI features stop when the plan lapses. Card changes and
invoices are self-serve through the Stripe customer portal ("Manage billing").

Only organization **owners and admins** can change billing; members can view the page.

A plan can also be **granted by Papervine** — a comp for a partner, a migration, or
support. A granted plan behaves exactly like a purchased one: the same features, the
same monthly credits, and the same self-serve controls (you can downgrade or change it
from Settings → Billing). A comp can be open-ended or time-boxed; a time-boxed one
lapses to Free on its own when it ends, just like a downgrade.

Running your own deployment? A deployment outside the hosted billing system has no
credit meter and brings its own model keys.
